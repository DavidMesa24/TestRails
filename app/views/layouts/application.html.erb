<!DOCTYPE html>
<html lang="en">
<head>
  <%= stylesheet_link_tag 'tailwind', 'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
  <%= javascript_importmap_tags %>

</head>
  <body class="gap-4">
    <header>
      <%= render "shared/navbar" %>
    </header>

    <div class="main-content flex justify-center items-center flex-col">
      <%= yield %>
    </div>

    <div class="flash-container mt-4 flex justify-center">
      <% flash.each do |key, value| %>
        <div class="flash-popup alert px-3 rounded-full <%= flash_class_for(key) %>">
          <span class="flash-close-btn" onclick="this.parentElement.style.display='none';">&times;</span>
          <%= value %>
        </div>
      <% end %>
    </div>

  </body>
</html>
