<% paths = action_paths_for_resource(resource) %>
<%= button_to paths[:delete], method: :delete, data: { confirm: 'Are you sure?' } do %>
  <%= image_tag(delete_icon, alt: 'Delete', class: 'cursor-pointer', style: 'width: 20px; height: 20px;') %>
<% end %>